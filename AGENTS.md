1. Call vibe_check after planning and before major actions.
2. Provide the full user request and your current plan.
3. Optionally, record resolved issues with vibe_learn.

- When explaining complex topics (e.g., new stacks, TypeScript internals, changing how the app works...), start with an ELI5-style summary before optional deeper details.
- If a fix attempt fails, restore the previous state or undo any trial commits so no dead or unused code remains (including temp flags, stubs, and TODO-only blocks).
