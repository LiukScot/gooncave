import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.gooncave.bundledprototype',
  appName: 'GoonCave Bundle Test',
  webDir: 'dist'
};

export default config;
